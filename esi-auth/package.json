{
  "name": "eve-jita-esi-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only ESI OAuth scripts (character orders, Perimeter structure market). Never deployed to Cloud Run — see README.md.",
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
